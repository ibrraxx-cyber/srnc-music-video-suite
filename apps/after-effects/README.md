# After Effects host adapter

This folder is intentionally separated from the shared preset engine.

The UI and preset schema are shared with Premiere, while After Effects execution will use an AE-compatible host adapter. The first production milestone is to implement `.ffx`/scripting application and a bridge for the same SRNC preset definitions.
